// PLACEHOLDER — replace by running `flutterfire configure` in the app/ folder.
// That command generates this file with your Firebase project's keys.
import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase is not configured yet. Run `flutterfire configure` in the '
      'app/ folder (see app/README.md).',
    );
  }
}
